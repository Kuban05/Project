using Library.Models;
using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;
using System.Web;

namespace Library.Formatters
{
    public class ListBooksFormatter
    {
        //It should write but something went wrong :D

        //public void WriteToFile(List<Book> model)
        //{
        //    string path = @"C:\ListBooks.txt";

        //    using (var sw = new StreamWriter(path))
        //    {
        //        if (model != null)
        //        {
        //            foreach (var item in model)
        //            {
        //                sw.WriteLine(item.Id + ";" + item.Title + ";" + item.Description + ";" + item.Year + ";" + item.Category + ";" + item.Author);
        //            }
        //        }
        //    }
        //}
    }
}